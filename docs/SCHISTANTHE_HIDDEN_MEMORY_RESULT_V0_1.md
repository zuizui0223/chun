# Prospective Schistanthe hierarchical hidden-memory result — v0.1

## Result

`PROSPECTIVE_SCHISTANTHE_HIDDEN_MEMORY_PASS`

A second independent held-out radiation now supports the hierarchical-memory prediction.

The full sequence was fixed before AUC opening:

1. candidate admitted by source metadata only;
2. exact trait and chronogram objects recovered and verified by Dryad SHA-256;
3. identifier-only crosswalk frozen;
4. 129 exact Tip_Label matches retained, with no alias/fuzzy repair;
5. fine/coarse state support opened;
6. opportunity confirmed: red 63 / white 36 / yellow 30 -> NONWHITE 93 / WHITE 36;
7. only then was the within-coarse hidden-memory AUC opened.

## Prospective test

Among pairs already sharing the same coarse WHITE/NONWHITE state:

- observed conditional AUC = **0.59646**
- within-coarse permutation null mean = **0.53230**
- centered hidden-memory effect = **+0.06416 AUC**
- null 95% interval = **0.49901–0.57678**
- 9,999 permutations
- one-sided **P = 0.0033**

PASS rule was frozen as:

> observed conditional AUC > permutation mean and one-sided P <= 0.05.

Both conditions are met.

## Why this matters

This result is qualitatively different from the post-outcome Flower-clades-51 synthesis.

It is an **independent radiation selected and admitted without opening its row-level target colors**, and the trait/tree/crosswalk/state-support sequence was frozen before the phylogenetic outcome.

Therefore the positive result directly supports the hierarchical-memory prediction:

> fine flower-color identity can preserve additional historical structure after coarse state membership is fixed.

It also supports the interpretation that global “best resolution” comparisons can miss nested structure.

## Boundary

This is a visible-color validation, not the pre-frozen biochemical Ruellia test.

Accordingly:

- it strengthens the hierarchical-memory model;
- it does **not** change the already-frozen Ruellia-specific EL v0.7 promotion rule;
- it does not establish a universal law;
- it does not identify the ecological or developmental mechanism generating hidden memory.

Frozen EL v0.3 and Camellia Paper 1 remain unchanged.
