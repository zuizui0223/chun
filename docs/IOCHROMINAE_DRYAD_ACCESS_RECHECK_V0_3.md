# Iochrominae Dryad source transport recheck — v0.3

## Result

`HOLD_DRYAD_SOURCE_BYTES_STILL_UNAVAILABLE_PROFILE_UNCOMPUTED`

The exact source objects remain identified from Dryad metadata:

- README — file id 108458, 4,072 bytes, MD5 `2e4a251725ad4c13975fc09481da302d`;
- DATA.rar — file id 108456, 3,054,999 bytes, MD5 `76b46e384fb7c9bf1ef3fbd7d1e5d2f0`.

The current recheck enumerated **all hrefs exposed by the live Dryad file metadata**, including relative hrefs, and also retried the legacy REST download and public file-stream routes.

Seven candidate URLs were tested for each object. None returned exact bytes satisfying both the frozen size and MD5.

## Firewall

Still unopened:

- archive member names;
- archive data rows;
- phenotype-state mapping;
- profile states;
- patristic distances;
- AUCs / permutations / winner.

The outcome directory remains absent.

## Interpretation

This is still a transport/access HOLD. It supplies no biological evidence against the Iochrominae biochemical profile or the hierarchical-memory hypothesis.

The next admissible step is exact recovery of **both** frozen source objects. Only then may the README and archive member names be inspected before freezing any state mapping.

Workflow: `35548348296` — PASS.  
Artifact: `10618005360`.

Frozen EL v0.3 and Camellia Paper 1 remain unchanged.
