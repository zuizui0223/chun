# Resolution-profile training inventory — v0.3

## Current exact-estimand count

The unconditional three-resolution AUC estimator has now been completed in **29 biological units**:

- **Iris** — 1 prospectively tested radiation; terminal result `FAIL` for the universal intermediate-ordering rule.
- **Flower-clades-51 batch** — 28 completed clades under one standardized visible-colour ontology.

The 28 standardized clades are the current quantitative training frame for tree-only moderator qualification because they share one representation type and one source protocol.

## Standardized 28-clade terminal distribution

| terminal class | count |
|---|---:|
| `PROFILE_SIGNALLED_COARSE` | 3 |
| `PROFILE_SIGNALLED_INTERMEDIATE` | 0 |
| `PROFILE_SIGNALLED_FINE` | 3 |
| `PROFILE_SIGNALLED_TIED` | 6 |
| `PROFILE_NO_PHYLOGENETIC_SIGNAL` | 16 |

Twenty-three additional clades from the same source are `HOLD_INSUFFICIENT_COMMON_FRAME_OR_STATE_VARIATION` and do not count as completed profile outcomes.

## Non-exchangeable / blocked systems

- **Gesnerioideae** — `HOLD_SCHEMA_SOURCE_AXES_NOT_NESTED`; not a biological negative.
- **Ruellia51 HPLC** — biochemical ladder preregistered and HPLC source fixed, but `HOLD_TREE_SOURCE_OUTCOMES_UNOPENED` because the exact timed tree used by author code is not present in Figshare v1/v2 or the public author GitHub tree.
- **Linoideae / Angraecinae / Antirrhineae conditional hierarchy analyses** — different conditional-null estimand; do not count toward the unconditional exact-profile total.

## Moderator status

The numerical minimum of five completed exact-estimand radiations is no longer limiting. However, moderator qualification is currently:

`NO_MODERATOR_QUALIFIED_CURRENT_STANDARDIZED_TRAINING`

Four pre-outcome tree-only candidate predictors were tested with one predictor plus intercept. No predictor improved leave-one-clade-out RMSE over intercept-only for both independent profile contrasts. Therefore no next held-out prediction is frozen yet.

## Scientific consequence

The programme has moved from:

> insufficient replication to test radiation-level heterogeneity

through:

> a prospective falsification of the universal intermediate optimum

to:

> **standardized replication showing heterogeneous resolution profiles that are not explained by simple tree geometry.**

The next information bottleneck is not sample size. It is **cross-representation training**: additional completed exact-profile systems whose nested representation is biochemical or otherwise non-visible-colour, so representation-level moderators can be identified prospectively.

Camellia Paper 1 remains unchanged.
